name: Apply order modal improvements (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Order modal improvements are applied through predev and prebuild."
